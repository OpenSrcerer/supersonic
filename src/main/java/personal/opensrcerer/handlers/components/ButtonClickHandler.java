package personal.opensrcerer.handlers.components;

import net.dv8tion.jda.api.events.interaction.ButtonClickEvent;
import personal.opensrcerer.handlers.Handler;

public class ButtonClickHandler implements Handler<ButtonClickEvent, ButtonClickEvent> {
    @Override
    public void handle(ButtonClickEvent p) {
    }
}
