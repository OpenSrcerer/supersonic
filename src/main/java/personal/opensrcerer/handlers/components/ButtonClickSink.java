package personal.opensrcerer.handlers.components;

import net.dv8tion.jda.api.events.interaction.ButtonClickEvent;
import personal.opensrcerer.handlers.Sink;

public class ButtonClickSink implements Sink<ButtonClickEvent, ButtonClickEvent> {
    @Override
    public void receive(ButtonClickEvent p) {
    }
}
